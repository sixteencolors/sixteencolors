# Based on PBP .perltidyrc file

-l=78   # Max line width is 78 cols
-i=4    # Indent level is 4 cols
-ci=4   # Continuation indent is 4 cols
-se     # Errors to STDERR
-vt=2   # Maximal vertical tightness
-cti=0  # No extra indentation for closing brackets
-pt=0   # Always 1 space near round brackets
-bt=0   # Always 1 space near curly brackets
-sbt=0  # Always 1 space near square brackets
-bbt=0  # Always 1 space block braces
-nsfs   # No space before semicolons
-nolq   # Don't outdent long quoted strings
-asc    # Add optional semi-colons
-wbb="% + - * / x != == >= <= =~ !~ < > | & >= < = **= += *= &= <<= &&= -= /= |= >>= ||= .= %= ^= x="
        # Break before all operators

